{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "//#lint": {
      "inputs": [
        "src/**/*.{js,jsx,ts,tsx}",
        "contracts/**/*.sol"
      ]
    }
  }
}