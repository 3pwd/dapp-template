{
  "name": "dapp-template",
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "dprint": "^0.47.2",
    "lefthook": "^1.7.5"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "fix": "bun run --filter '*' fix",
    "test": "bun run --filter '*' test"
  },
  "workspaces": ["contracts", "ui"]
}
